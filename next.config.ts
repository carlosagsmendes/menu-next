import type { NextConfig } from "next";

/**
 * App Router uses React Server Components by default for `app/` layouts and pages.
 * Client boundaries are opt-in via the `"use client"` directive (e.g. `components/SideNav.tsx`).
 */
const nextConfig: NextConfig = {};

export default nextConfig;
